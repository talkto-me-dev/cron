name: deploy pipeline

env:
  TZ: Asia/Shanghai

on:
  workflow_dispatch:
    inputs:
      env:
        description: target env
        required: true
        type: choice
        options: [alpha, prod]

concurrency:
  group: deploy-pipeline-${{ inputs.env }}
  cancel-in-progress: false

jobs:
  run:
    uses: ./.github/workflows/cron.yml
    with:
      name: deploy_pipeline
      env: ${{ inputs.env }}
    secrets: inherit
