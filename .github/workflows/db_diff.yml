name: db_diff

env:
  TZ: Asia/Shanghai

on:
  workflow_dispatch:
    inputs:
      env:
        description: target env
        required: true
        type: choice
        options: [alpha, prod]

jobs:
  run:
    uses: ./.github/workflows/cron.yml
    with:
      name: db_diff
      env: ${{ inputs.env }}
    secrets: inherit
